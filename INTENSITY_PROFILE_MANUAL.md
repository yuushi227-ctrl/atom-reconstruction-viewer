# 強度プロファイル機能 説明書
## `atomic_image_reconstruction_viewer_v3.py` — INTENSITY PROFILE パネル

---

## 目次
1. [機能概要](#1-機能概要)
2. [GUI パネルの各項目](#2-gui-パネルの各項目)
3. [操作手順（クイックスタート）](#3-操作手順クイックスタート)
4. [アルゴリズム詳細](#4-アルゴリズム詳細)
5. [重み付けモードの選び方](#5-重み付けモードの選び方)
6. [結果の見方](#6-結果の見方)
7. [API 連携動作](#7-api-連携動作)
8. [トラブルシューティング](#8-トラブルシューティング)
9. [パラメータ推奨値（Pb 原子の場合）](#9-パラメータ推奨値pb-原子の場合)

---

## 1. 機能概要

**INTENSITY PROFILE** 機能は、読み込み済みの XYZ ファイル（原子サイト座標）を参照し、
指定した Z 範囲の全フレームを 1 枚ずつ走査して「**原子サイト領域の輝度総和**」を計算します。

計算が完了すると：

- 横軸 Z(Å)・縦軸 強度総和 の **折れ線グラフ**を別ウィンドウで表示
- 強度総和が最大になる **最適 Z 位置**を自動特定
- ビューアの **Z スライダーを最適位置へ自動移動**
- 3D-AIR-IMAGE API が接続されていれば、外部アプリの **シリーズフレームも同期移動**

---

## 2. GUI パネルの各項目

左パネル最下部の **「▸ INTENSITY PROFILE (原子サイト自動検出)」** グループに以下の項目があります。

### 2-1. Z 走査範囲

| 項目 | 説明 |
|------|------|
| **Z 開始 (Å)** | 走査を開始する Z 座標（デフォルト: 4.6 Å） |
| **Z 終了 (Å)** | 走査を終了する Z 座標（デフォルト: 5.4 Å） |

ボリュームデータの Z 範囲内に収まるよう設定してください。  
VOXEL SIZE の `dz` × フレーム数 = ボリュームの Z 幅です。

---

### 2-2. 原子半径 (Å) と → px 変換

| 項目 | 説明 |
|------|------|
| **原子半径 (Å)** | サンプリングに使う物理的な原子半径（デフォルト: 0.80 Å） |
| **→ px 変換ボタン** | `round(原子半径 ÷ dx)` を計算し、下のサンプリング半径 (px) へ反映 |

**計算式：**
```
radius_px = round(原子半径[Å] ÷ dx[Å/px])
```

例：`原子半径 = 0.80 Å`、`dx = 0.1 Å/px` → `radius_px = 8 px`

> **ガウシアンモード**では、この値が σ（標準偏差）として使われます。  
> → px 変換ボタンを押すと、σ の 3 倍程度の値をサンプリング半径に設定するのが推奨です（詳細は §5）。

---

### 2-3. サンプリング半径 (px)

| 項目 | 説明 |
|------|------|
| **サンプリング半径 (px)** | 各原子サイト中心から何ピクセル以内を積算するか（範囲: 1〜50） |

- **フラットモード**：この半径内のピクセルを一様に積算します
- **ガウシアンモード**：この値は「打ち切り半径」として機能します（σ の 3 倍以上を推奨）

---

### 2-4. 重み付け

| 選択肢 | 動作 |
|--------|------|
| **ガウシアン（推奨）** | 中心から離れるほど重みが滑らかに減衰。カットオフ半径への依存が極めて小さい。 |
| **一様（フラットディスク）** | 半径内のピクセルを均等に積算。境界ピクセルの有無で結果が変わる場合あり。 |

---

### 2-5. 最適Z位置の自動検出＆プロット ボタン

クリックすると計算を開始します。  
進捗ダイアログが表示され、キャンセルも可能です。

---

### 2-6. 最適 Z 結果ラベル

計算完了後に以下が表示されます：

```
最適Z: 4.950 Å
フレーム 35 / 80
強度総和: 1.234e+05
```

---

## 3. 操作手順（クイックスタート）

### ステップ 1：ボリュームデータを読み込む

- **Series → 3D ボリューム構築** で、3D-AIR-IMAGE のシリーズウィンドウを選択して読み込む
- または **ファイルから読み込み**（.npy / .npz / .xml_air）

> シリーズから読み込んだ場合、ウィンドウ ID が自動保存されます（API 連携に使用）。

---

### ステップ 2：XYZ ファイルを読み込む

- **XYZ 読み込み (複数可)** ボタンで `Cluster_00.xyz` を読み込む
- リストで `Cluster_00` が選択されアクティブ状態になっていることを確認

---

### ステップ 3：ボクセルサイズを確認する

- VOXEL SIZE パネルの **dx** の値を確認する
- `dx = 0.1 Å/px` の場合、Pb の 0.80 Å は 8 px に相当

---

### ステップ 4：原子半径を px に変換する

1. INTENSITY PROFILE パネルの **原子半径 (Å)** に `0.80` を入力（デフォルト値）
2. **→ px 変換** ボタンをクリック
3. **サンプリング半径 (px)** に変換値が自動入力される

> **ガウシアンモードの場合**は、サンプリング半径を σ の 3 倍以上に手動設定してください。  
> 例：σ = 8 px なら、サンプリング半径を 24〜30 px に設定。

---

### ステップ 5：Z 走査範囲を設定する

- **Z 開始** / **Z 終了** を設定（例：4.6〜5.4 Å）
- Pb の原子像が見えるフレーム付近を指定する

---

### ステップ 6：重み付けを選ぶ

- 通常は **ガウシアン（推奨）** を選択

---

### ステップ 7：実行する

- **最適Z位置の自動検出＆プロット** ボタンをクリック
- 進捗バーが表示され、計算が完了するとグラフウィンドウが開く
- Z スライダーが自動的に最適位置へ移動する

---

## 4. アルゴリズム詳細

### 4-1. 全体フロー

```
① XYZ ファイルから原子の (x, y) 座標 [Å] を取得
        ↓
② (x, y) [Å] → (ix, iy) [pixel] に変換
   ix = round((x - x_range[0]) / dx)
   iy = round((y - y_range[0]) / dy)
        ↓
③ Z 開始〜Z 終了に対応するフレームインデックス範囲を計算
   zi = round((z[Å] - z_range[0]) / dz)
        ↓
④ 各フレーム iz を 1 枚ずつ処理：
   ┌─ フレーム画像 frame = volume[iz]
   │  各原子サイト (cx, cy) について
   │   └─ サイト中心周囲のピクセルを重み付き積算
   └─ 全原子サイトの総和 → intensity_sum
        ↓
⑤ 結果リスト [(z_ang, intensity_sum), ...] を返す
        ↓
⑥ argmax でピーク特定 → スライダー移動 + グラフ描画
```

---

### 4-2. ピクセル積算の方法

各原子サイト中心 `(cx, cy)` に対して、周囲のピクセルを以下のように積算します。

#### フラットディスク（一様）

```
mask: dx² + dy² ≤ radius_px²
intensity_sum += Σ frame[cy+dy, cx+dx]  (mask 内の全ピクセル)
```

#### ガウシアン重み付き

```
σ = 原子半径[Å] ÷ dx[Å/px]
weight(dx, dy) = exp( −(dx² + dy²) / (2σ²) )
打ち切り: weight < 0.01 max のピクセルは除外

intensity_sum += Σ frame[cy+dy, cx+dx] × weight(dx, dy)
```

---

### 4-3. Z 座標とフレームインデックスの対応

ボリュームデータは原点中心で格納されています：

```
z_range = (−Nz×dz/2, +Nz×dz/2)
z_ang[iz] = z_range[0] + iz × dz
iz = round((z_ang − z_range[0]) / dz)
```

---

## 5. 重み付けモードの選び方

### なぜフラットディスクで結果がばらつくのか

フラットディスクは「入るか入らないか」の 0/1 判定です。

```
半径 8 px → 201 ピクセルを積算
半径 9 px → 253 ピクセルを積算（+52 ピクセル）
```

この境界付近に明るいピクセルがあると、積算に入った瞬間に強度総和がジャンプします。
ユーザーが観察した「8px → Z=5.1 Å、9px → Z=4.95 Å」という変化はこれが原因です。

### ガウシアン重み付けが安定な理由

```
σ = 8 px のガウシアンの場合：
 r = 8 px  → weight = exp(−32) ≈ 0.37  (まだ値あり)
 r = 16 px → weight = exp(−128) ≈ 0.00 (ほぼゼロ)
```

半径を 20 px にしても 40 px にしても、追加されるピクセルの重みはほぼゼロなので
積算値は変わりません。

### 推奨設定（Pb の場合）

| パラメータ | 推奨値 |
|-----------|--------|
| 原子半径 (Å) | 0.80 |
| σ (= → px 変換結果) | 8 px（dx=0.1 Å 時） |
| サンプリング半径 (px) | 24〜30（σ の 3〜4 倍） |
| 重み付け | **ガウシアン** |

---

## 6. 結果の見方

### グラフウィンドウ

グラフタイトルに使用した重み付けモードが表示されます：
```
原子サイト強度プロファイル  [ガウシアン重み付き  σ = 0.80 Å]
```

| 要素 | 説明 |
|------|------|
| **青い折れ線** | 各フレームの強度総和 |
| **赤い破線** | 最適 Z 位置 |
| **赤い ★** | 強度総和のピーク点 |
| X 軸 | Z 座標 (Å) |
| Y 軸 | 強度総和 (a.u.) |

### 解釈のポイント

- **シャープなピーク**：特定の Z に原子が集中している（良好なデータ）
- **ブロードなピーク**：原子が Z 方向に広がっている、またはデータが粗い
- **複数のピーク**：複数の原子層が存在する可能性
- **ノイズが多い**：サンプリング半径を小さくしてみる、またはガウシアン σ を小さくする

---

## 7. API 連携動作

3D-AIR-IMAGE に接続済みで、かつ **Series → 3D ボリューム構築** で読み込んだ場合に有効です。

```
自動検出後：
  1. ビューア内の Z スライダーを最適位置へ移動（常時）
  2. air.series_imageindex_set(series_wid, frame_idx) を呼び出し
     → 3D-AIR-IMAGE アプリのシリーズ表示も同期
```

ファイルから読み込んだ場合（API 未接続）は、ステップ 2 はスキップされます（エラーにはなりません）。

---

## 8. トラブルシューティング

### 「対象となる原子サイトがありません」

- XYZ ファイルが読み込まれていない → **XYZ 読み込み** ボタンから読み込む
- 読み込んだ XYZ セットが非表示になっている → リストのチェックボックスをオンにする

### 「原子サイトがボリュームの X/Y 範囲外です」

- XYZ ファイルの座標がボリュームの範囲と合っていない
- ATOM OFFSET パネルで X/Y オフセットを調整する
- ボクセルサイズ（dx, dy）が正しいか確認する

### 「指定した Z 範囲内にフレームが存在しません」

- Z 開始・終了がボリュームの Z 範囲外になっている
- `lbl_volume_info` に表示されている「サイズ」と `dz` から Z 範囲を確認する

### グラフが表示されない

- matplotlib の Qt5Agg バックエンドが正常に動作していない可能性
- Python コンソールでエラーを確認する

### 結果が Z 範囲の端に張り付く

- Z 走査範囲が狭すぎる → Z 開始/終了の範囲を広げる
- 原子のピークが設定範囲の外にある可能性

---

## 9. パラメータ推奨値（Pb 原子の場合）

| 条件 | dx | σ (px) | サンプリング半径 (px) | 重み |
|------|-----|--------|----------------------|------|
| 高分解能 (dx=0.05 Å) | 0.05 | 16 | 48 | ガウシアン |
| 標準 (dx=0.1 Å) | 0.10 | 8 | 24〜30 | ガウシアン |
| 粗い (dx=0.2 Å) | 0.20 | 4 | 12〜15 | ガウシアン |

> Pb の原子半径 0.80 Å を使用。他の元素の場合は元素半径に応じて調整。

---

*作成日: 2026-07-06*  
*対象ファイル: `atomic_image_reconstruction_viewer_v3.py`*
